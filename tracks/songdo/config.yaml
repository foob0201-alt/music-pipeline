# ============================================================
# 곡별 override — "송도유원지"
# 루트 config.yaml 기본값 위에 _deep_merge 로 덮어씀 (지정 항목만)
# 자막/영상 표준(font 110 · scale 1.15 · dual 315·201 · 1440p · CRF16)은 루트 config 상속.
# ============================================================

track:
  name: songdo
  dir: tracks/songdo
  # ---- 창작 메모(파이프라인 비참조) ----
  place: "인천 송도유원지 — 처음 이사 온 동네"
  # 모티프: 별로였던 낯선 동네 → 네 향기로 온통 덮임(장소=감정 은유). 수미상관: 첫 '낯섦'을 말미 '온통 너'로 되울림

meta:
  title: "송도유원지"
  artist: "Reina"
  # ---- 창작 메모(파이프라인 비참조) ----
  # title_en: "Songdo Yuwonji"
  # style(Suno Style 필드용): warm romantic K-pop/R&B ballad, 인천 로컬 노스탤지어

paths:
  audio: songdo.mp3
