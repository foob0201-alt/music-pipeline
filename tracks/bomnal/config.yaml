# ============================================================
# 곡별 override — "봄날"
# 루트 config.yaml 기본값 위에 _deep_merge 로 덮어씀 (지정 항목만)
# 자막 시작값(font 110 / dual_ko_margin 315 / dual_en_margin 201)은 루트 config 사용.
# ============================================================

track:
  name: bomnal
  dir: tracks/bomnal
  # ---- 창작 메모(파이프라인 비참조) ----
  place: "그 봄날의 방 · 너희 집 앞 골목 (여름비)"

meta:
  title: "봄날"
  artist: "영석고고"

paths:
  audio: bomnal.mp3
