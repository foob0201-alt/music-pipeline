# ============================================================
# 곡별 override — "거울 속의 오늘"
# 루트 config.yaml 기본값 위에 _deep_merge 로 덮어씀 (지정 항목만)
# 자막/영상 표준(font 110 / dual 315·201 / 1440p / crf16)은 루트 config 사용.
# ============================================================

track:
  name: geoul_oneul
  dir: tracks/geoul_oneul
  # ---- 창작 메모(파이프라인 비참조) ----
  place: "거울 앞 · 소주 한 잔 (잔상·Afterimage)"

meta:
  title: "거울 속의 오늘"
  artist: "영석고고"

paths:
  audio: geoul_oneul.mp3
